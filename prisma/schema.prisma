// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["safehouse", "auth"]
}

model Folder {
  id         String    @id @default(cuid())
  name       String
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt
  meetings   Meeting[]
  @@map("talktome_folders")
  @@schema("safehouse")
}

model Meeting {
  id             String    @id @default(cuid())
  title          String
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  scheduledAt    DateTime?
  durationSec    Int?
  audioUrl       String?
  transcript     String?
  summary        String?
  wordsPerMinute Int?
  speakers       Int?
  folder         Folder?   @relation(fields: [folderId], references: [id])
  folderId       String?
  chunks         TranscriptChunk[]
  edits          TranscriptEdit[]
  bookmarks      Bookmark[]
  importJobs     ImportJob[]
  @@map("talktome_meetings")
  @@schema("safehouse")
}

model TranscriptChunk {
  id         String   @id @default(cuid())
  meeting    Meeting  @relation(fields: [meetingId], references: [id])
  meetingId  String
  startMs    Int
  endMs      Int
  text       String
  speaker    String? // simple label for now
  createdAt  DateTime @default(now())
  @@map("talktome_transcript_chunks")
  @@schema("safehouse")
}

model TranscriptEdit {
  id         String   @id @default(cuid())
  meeting    Meeting  @relation(fields: [meetingId], references: [id])
  meetingId  String
  fromText   String
  toText     String
  createdAt  DateTime @default(now())
  @@map("talktome_transcript_edits")
  @@schema("safehouse")
}

model Bookmark {
  id         String   @id @default(cuid())
  meeting    Meeting  @relation(fields: [meetingId], references: [id])
  meetingId  String
  label      String
  timeMs     Int
  createdAt  DateTime @default(now())
  @@map("talktome_bookmarks")
  @@schema("safehouse")
}

model ImportJob {
  id         String   @id @default(cuid())
  meeting    Meeting? @relation(fields: [meetingId], references: [id])
  meetingId  String?
  fileName   String
  fileUrl    String
  status     String   @default("queued") // queued|processing|done|error
  error      String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  @@map("talktome_import_jobs")
  @@schema("safehouse")
}
